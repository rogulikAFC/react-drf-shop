function Products({ products }) {
    return (
        <div className="main__products">
            {products.map(product => (
                <div className="h2">ladno</div>
            ))}
        </div>
    )
}